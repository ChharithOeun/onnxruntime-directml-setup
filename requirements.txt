# ONNX Runtime DirectML — AMD GPU inference on Windows
# Install with: pip install -r requirements.txt

# ONNX Runtime with DirectML execution provider
# NOTE: mutually exclusive with plain 'onnxruntime' — only install one
onnxruntime-directml>=1.17.0

# ONNX model format support
onnx>=1.15.0

# HuggingFace model export and optimized inference
optimum[exporters]>=1.17.0
transformers>=4.40.0
accelerate>=0.27.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Audio processing (for Whisper task)
librosa>=0.10.0
soundfile>=0.12.0

# Utilities
huggingface-hub>=0.20.0
tqdm>=4.66.0
requests>=2.31.0
